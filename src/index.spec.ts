import 'should';

describe('Testing test', () => {
    it('should true the true', () => {
        true.should.true();
    });

    it('should false the false', () => {
        true.should.false();
    });
});
